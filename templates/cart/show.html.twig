{% extends 'base.html.twig' %}

{% block title %}Panier{% endblock %}

{% block body %}
    <h1>Votre panier</h1>

    <a class="btn btn-dark mb-3" href="{{ path('products_list') }}">Retour</a>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cart.id }}</td>
            </tr>
            <tr>
                <th>Quantité</th>
                <td>{{ cart.quantity }}</td>
            </tr>
            <tr>
                <th>Montant total</th>
                <td>{{ cart.totalAmount }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-dark" href="{{ path('cart_edit', {'id': cart.id}) }}">Modifier</a>

    {{ include('cart/_delete_form.html.twig') }}
{% endblock %}
