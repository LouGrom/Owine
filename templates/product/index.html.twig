{% extends 'base.html.twig' %}

{% block title %}Product index{% endblock %}

{% block body %}
    <h1>Product index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Appellation</th>
                <th>Area</th>
                <th>Type</th>
                <th>CuveeDomaine</th>
                <th>Capacity</th>
                <th>Vintage</th>
                <th>Color</th>
                <th>AlcoholVolume</th>
                <th>Price</th>
                <th>HsCode</th>
                <th>Description</th>
                <th>Picture</th>
                <th>Status</th>
                <th>StockQuantity</th>
                <th>CreatedAt</th>
                <th>UpdatedAt</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for product in products %}
            <tr>
                <td>{{ product.id }}</td>
                <td>{{ product.appellation }}</td>
                <td>{{ product.area }}</td>
                <td>{{ product.type }}</td>
                <td>{{ product.cuveeDomaine }}</td>
                <td>{{ product.capacity }}</td>
                <td>{{ product.vintage }}</td>
                <td>{{ product.color }}</td>
                <td>{{ product.alcoholVolume }}</td>
                <td>{{ product.price }}</td>
                <td>{{ product.hsCode }}</td>
                <td>{{ product.description }}</td>
                <td>{{ product.picture }}</td>
                <td>{{ product.status }}</td>
                <td>{{ product.stockQuantity }}</td>
                <td>{{ product.createdAt ? product.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ product.updatedAt ? product.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('product_show', {'id': product.id}) }}">Détails</a>
                    <a href="{{ path('product_edit', {'id': product.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="18">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('product_new') }}">Ajouter</a>
{% endblock %}
